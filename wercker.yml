
box: docker:latest

build:
  docker: true
  steps:
    - script:
        code: |
          echo "test"
    - script:
        code: |
          #service docker start
          docker login --username="$DOCKERHUB_USERNAME" --password="$DOCKERHUB_PASSWORD"
          docker build --tag="gazf/ubuntu-docker" .
          docker push gazf/ubuntu-docker
          
