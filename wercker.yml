
box: docker:latest

build:
  docker: true
  steps:
    - script:
        code: |
          echo "test"
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: ubuntu-docker
    - script:
        code: |
          #service docker start
          docker login --username="$DOCKERHUB_USERNAME" --password="$DOCKERHUB_PASSWORD"
          docker push ubuntu-docker:latest
          
